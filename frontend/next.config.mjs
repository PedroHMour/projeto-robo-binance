/** @type {import('next').NextConfig} */
const nextConfig = {
  // A configuração para desativar o Turbopack no Next.js 15 mudou.
  // Agora, simplesmente não usamos a flag --turbopack no comando de dev.
  // Podemos deixar este arquivo mais limpo.
};

export default nextConfig;